---
export interface Props {
  type?: 'button' | 'submit' | 'reset';
  class?: string;
}

const { type = 'button', class: className = '' } = Astro.props;
---

<button type={type} class={`ink-button ${className}`}>
  <slot />
</button>
